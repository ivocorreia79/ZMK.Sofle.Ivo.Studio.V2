name: Build ZMK firmware

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: urob/zmk-actions/.github/workflows/build-user-config.yml@v3
    with:
      config_path: config
      build_matrix_path: build.yaml
      archive_name: ZMK.Sofle.Ivo.Studio.firmware
      run-in-container: true
